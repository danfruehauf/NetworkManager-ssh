---
name: Build NetworkManager-ssh Ubuntu Latest

on:
  push:
    branches: [main]
  pull_request:
    branches: ["**"]

jobs:
  call-build:
    uses: ./.github/workflows/build.yml
    with:
      image-name: ubuntu
      image-version: latest
